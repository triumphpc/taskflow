## 1. Перенос поля ввода в окно

- [x] 1.1 Убрать вызов `composer()` из `renderListView` в `js/ui.js`
- [x] 1.2 Завести `openComposer()`: `openSheet({ title: 'Новая задача' })` с телом
      из существующего `composer()`
- [x] 1.3 Установить фокус в поле **синхронно**, до возврата из обработчика жеста —
      без `setTimeout` и `requestAnimationFrame`
- [x] 1.4 После Enter очищать поле и оставлять окно открытым, фокус — в поле
- [x] 1.5 Сохранить тост с действием «Открыть» после добавления
- [x] 1.6 Убрать `focusComposer()` и все его вызовы

## 2. Кнопка «+»

- [x] 2.1 Показать `.fab` на всех размерах экрана в `styles.css`
- [x] 2.2 Разместить кнопку в правом нижнем углу области списка на широких экранах
- [x] 2.3 Завести нижний отступ списка на широких экранах, чтобы кнопка не перекрывала
      последнюю задачу
- [x] 2.4 Упростить обработчик `#btn-fab` в `js/app.js`: только `openComposer()`,
      без переключения раздела
- [x] 2.5 Перевести клавишу `n` в `onKeydown` на `openComposer()`

## 3. Анимация

- [x] 3.1 Добавить класс-модификатор окна с ростом от правого нижнего угла:
      `transform-origin`, `scale` от ~0.9, `opacity`
- [x] 3.2 Не трогать общую анимацию `rise` — остальные окна открываются как раньше
- [x] 3.3 Оставить выезд снизу на узких экранах
- [x] 3.4 Проверить, что глобальное правило `prefers-reduced-motion` покрывает новую анимацию

## 4. Тексты

- [x] 4.1 Поправить пустые состояния, отсылающие к полю ввода сверху
- [x] 4.2 Обновить описание добавления задачи в `README.md`
- [x] 4.3 Обновить описание в README для агента, если оно упоминает поле над списком

## 5. Проверка

- [x] 5.1 Широкий экран: список без формы, «+» виден, окно открывается
- [x] 5.2 Переключение всех разделов: поля ввода нет нигде
- [x] 5.3 Три задачи подряд без повторного открытия окна
- [x] 5.4 Быстрый синтаксис «завтра 18:30 !1» разбирается как раньше
- [x] 5.5 Дата по умолчанию берётся из раздела, из которого открыли окно
- [x] 5.6 Окно открывается из «Выполнено» и не переключает раздел
- [x] 5.7 Esc, крестик и клик мимо закрывают окно
- [x] 5.8 Длинный список: последняя задача не уходит под кнопку
- [x] 5.9 Телефон: клавиатура поднимается с первого касания, страница не масштабируется
- [x] 5.10 Клавиша `n` открывает то же окно

## 6. Завершение

- [x] 6.1 Поднять `CACHE` в `sw.js`
