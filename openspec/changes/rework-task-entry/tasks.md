## 1. Перенос поля ввода в окно

- [ ] 1.1 Убрать вызов `composer()` из `renderListView` в `js/ui.js`
- [ ] 1.2 Завести `openComposer()`: `openSheet({ title: 'Новая задача' })` с телом
      из существующего `composer()`
- [ ] 1.3 Установить фокус в поле **синхронно**, до возврата из обработчика жеста —
      без `setTimeout` и `requestAnimationFrame`
- [ ] 1.4 После Enter очищать поле и оставлять окно открытым, фокус — в поле
- [ ] 1.5 Сохранить тост с действием «Открыть» после добавления
- [ ] 1.6 Убрать `focusComposer()` и все его вызовы

## 2. Кнопка «+»

- [ ] 2.1 Показать `.fab` на всех размерах экрана в `styles.css`
- [ ] 2.2 Разместить кнопку в правом нижнем углу области списка на широких экранах
- [ ] 2.3 Завести нижний отступ списка на широких экранах, чтобы кнопка не перекрывала
      последнюю задачу
- [ ] 2.4 Упростить обработчик `#btn-fab` в `js/app.js`: только `openComposer()`,
      без переключения раздела
- [ ] 2.5 Перевести клавишу `n` в `onKeydown` на `openComposer()`

## 3. Анимация

- [ ] 3.1 Добавить класс-модификатор окна с ростом от правого нижнего угла:
      `transform-origin`, `scale` от ~0.9, `opacity`
- [ ] 3.2 Не трогать общую анимацию `rise` — остальные окна открываются как раньше
- [ ] 3.3 Оставить выезд снизу на узких экранах
- [ ] 3.4 Проверить, что глобальное правило `prefers-reduced-motion` покрывает новую анимацию

## 4. Тексты

- [ ] 4.1 Поправить пустые состояния, отсылающие к полю ввода сверху
- [ ] 4.2 Обновить описание добавления задачи в `README.md`
- [ ] 4.3 Обновить описание в README для агента, если оно упоминает поле над списком

## 5. Проверка

- [ ] 5.1 Широкий экран: список без формы, «+» виден, окно открывается
- [ ] 5.2 Переключение всех разделов: поля ввода нет нигде
- [ ] 5.3 Три задачи подряд без повторного открытия окна
- [ ] 5.4 Быстрый синтаксис «завтра 18:30 !1» разбирается как раньше
- [ ] 5.5 Дата по умолчанию берётся из раздела, из которого открыли окно
- [ ] 5.6 Окно открывается из «Выполнено» и не переключает раздел
- [ ] 5.7 Esc, крестик и клик мимо закрывают окно
- [ ] 5.8 Длинный список: последняя задача не уходит под кнопку
- [ ] 5.9 Телефон: клавиатура поднимается с первого касания, страница не масштабируется
- [ ] 5.10 Клавиша `n` открывает то же окно

## 6. Завершение

- [ ] 6.1 Поднять `CACHE` в `sw.js`
