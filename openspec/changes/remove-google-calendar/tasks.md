## 1. Уборка в Google (до кода, необратимо)

- [x] 1.1 Открыть приложение с включённой синхронизацией, дождаться, что очередь выгрузки пуста и `state.pendingDeletes` разобран
- [x] 1.2 Просмотреть календарь «TaskFlow» в Google глазами, перенести в основной календарь события, которые нужны как настоящие встречи
- [x] 1.3 Удалить календарь «TaskFlow» в Google Calendar целиком, вместе с событиями
- [x] 1.4 Удалить OAuth Client ID в Google Cloud Console (или весь проект `taskflow`)

## 2. Удаление модуля и его точек входа

- [x] 2.1 Удалить файл `js/gcal.js` (447 строк)
- [x] 2.2 Убрать `import * as G from './gcal.js'` из `js/ui.js:8` и `js/app.js:7`
- [x] 2.3 Убрать из `js/app.js` восстановление сессии `G.restoreSession()` (стр. 143–144), ручную синхронизацию по кнопке (стр. 123–126) и запуск `scheduleSync()` при возврате во вкладку (стр. 156)
- [x] 2.4 Убрать из `js/app.js` привязку клавиши `5` к календарю (стр. 77); назначить `5` на `someday` и добавить `6` на `done`

## 3. Интерфейс: `js/ui.js`

- [x] 3.1 Убрать `'calendar'` из `NAV_ITEMS` (стр. 82) и `NAV_MOBILE` (стр. 83)
- [x] 3.2 Убрать заголовок раздела «Google Calendar» (стр. 126)
- [x] 3.3 Убрать метки `⚠ Google` и `◉` из строки задачи (стр. 168–169)
- [x] 3.4 Убрать упоминание удаления событий из предупреждения при очистке выполненных (стр. 601) и восстановление поля `gcal` при отмене (стр. 609)
- [x] 3.5 Убрать автоэкспорт после правок (стр. 619–622)
- [x] 3.6 Удалить `gcalLine()` и его вставку в редактор задачи (стр. 843–848, 864)
- [x] 3.7 Удалить `renderCalendarView` (стр. 911–966) вместе с вкладками и блоком про HTTPS
- [x] 3.8 Удалить `parseEmbedIds` и `embedWidget` (стр. 974–998)
- [x] 3.9 Удалить блок настроек Google целиком (стр. 1040–1160): Client ID, имя календаря, длительность события, отзыв доступа, список календарей, id для виджета, режим виджета

## 4. Данные и состояние

- [x] 4.1 Убрать `VIEWS.calendar` из `js/model.js:230`
- [x] 4.2 Убрать складывание удалённых событий в `state.pendingDeletes` (`js/model.js:78–79`)
- [x] 4.3 Убрать сброс `eventId` при переносе повторяющейся задачи (`js/model.js:155`)
- [x] 4.4 Убрать само поле `pendingDeletes` из состояния: `js/store.js:36`, `77`, `100`, `215`
- [x] 4.5 Убрать девять настроек из `js/store.js:11–19` — `googleClientId`, `gcalEnabled`, `gcalCalendarName`, `gcalCalendarId`, `gcalEventMinutes`, `gcalAutoSync`, `hiddenCalendarIds`, `embedCalendarIds`, `embedMode` (исходный пропозал насчитал восемь, `embedMode` был пропущен)
- [x] 4.6 Убрать блок `gcal` в `normalizeTask` (`js/store.js:132–136`) и `googleClientId` из экспорта настроек (стр. 164)
- [x] 4.7 Убедиться, что `normalizeTask` не падает на задаче с полем `gcal` в данных и не удаляет его принудительно

## 5. Оболочка, стили, документация

- [x] 5.1 Убрать `'./js/gcal.js'` из `SHELL` в `sw.js` и поднять `CACHE` с `taskflow-v10` до `taskflow-v11`
- [x] 5.2 Удалить стили `.task-meta .synced`, `.task-meta .syncerr` (`styles.css:313–314`), `.cal-tabs`, `.cal-day`, `.cal-day h3` (стр. 569–586), `.embed-wrap`, `.embed-wrap iframe` (стр. 587–591)
- [x] 5.3 Поправить `index.html`: упоминание Google Calendar в `<meta name="description">` (стр. 7) и подпись кнопки `#btn-sync` (стр. 49) — кнопка теперь синхронизирует только с собственным сервером
- [x] 5.4 Убрать из `README.md` и `README.ru.md` раздел «Connecting Google Calendar» целиком, упоминания в списке возможностей и в разделе про HTTPS
- [x] 5.5 Поправить строку про горячие клавиши в `README.ru.md:35` — диапазон `1`…`6`

## 6. Проверка

- [x] 6.1 `grep -rin "gcal\|google" js/ index.html styles.css sw.js` возвращает пусто
- [x] 6.2 Приложение открывается без ошибок в консоли; ни один запрос не уходит на домены Google
- [x] 6.3 Клавиши `1`–`6` открывают разделы, ни одна не ведёт в несуществующий
- [x] 6.4 Задача, у которой в `data/taskflow.json` осталось поле `gcal`, отображается нормально; после правки поле уходит из записи
- [ ] 6.5 Проверить на телефоне, что service worker обновился на `taskflow-v11` и не пытается загрузить удалённый `js/gcal.js`
- [ ] 6.6 Синхронизация между Mac и телефоном работает, `SCHEMA` остался `1`
