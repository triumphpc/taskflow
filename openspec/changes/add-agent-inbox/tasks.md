## 1. Модель данных

- [ ] 1.1 Добавить в `normalizeTask` (`js/store.js`) поле `kind` со значением `'task'` по умолчанию, если поле отсутствует
- [ ] 1.2 Добавить нормализацию `source` вида `{ kind, url, title, ref, at }` с допустимыми `kind`: `gmail`, `telegram`, `web`, `calendar`, `agent`, `manual`
- [ ] 1.3 Добавить нормализацию `agentNotes` как массива `{ at, text }`; пустой массив по умолчанию
- [ ] 1.4 Убедиться, что `SCHEMA` осталась `1` и `sync.mjs` не потребовал правок
- [ ] 1.5 Проверить, что запись без `kind`, созданная до этой работы, читается как задача

## 2. Выборки и раздел

- [ ] 2.1 Добавить `VIEWS.inbox` в `js/model.js` с заголовком «Входящие»
- [ ] 2.2 Исключить записи с `kind: 'inbox'` из всех остальных разделов — они не должны появляться среди задач ни в одном из них
- [ ] 2.3 Добавить выборку неразобранных входящих и счётчик для навигации
- [ ] 2.4 Реализовать превращение входящего в задачу: `kind` → `'task'`, `source` и `agentNotes` сохраняются

## 3. Интерфейс

- [ ] 3.1 Добавить `'inbox'` в `NAV_ITEMS` между `today` и `tomorrow`, и в `NAV_MOBILE` после `today`
- [ ] 3.2 Показать счётчик неразобранных у пункта навигации; скрывать счётчик, когда разбирать нечего
- [ ] 3.3 Привязать клавишу `7` к разделу `inbox`, не трогая `1`–`6`
- [ ] 3.4 Сделать карточку входящего: источник с датой, заголовок, лента `agentNotes`, действия «В задачу», «Отложить», «Отбросить», переход к источнику `↗`
- [ ] 3.5 Показывать `agentNotes` и `notes` раздельно, различимо по происхождению
- [ ] 3.6 Реализовать «В задачу» — открывает обычный редактор с датой и приоритетом
- [ ] 3.7 Реализовать «Отбросить» — обычное удаление с надгробием и отменой через тост
- [ ] 3.8 Добавить стили карточки в `styles.css`
- [ ] 3.9 Поднять `CACHE` в `sw.js`

## 4. Moments

- [ ] 4.1 Добавить второй акт: после задач на сегодня — разбор входящих
- [ ] 4.2 Набор ответов второго акта: «В задачу», «Отложить», «Отбросить»
- [ ] 4.3 Пропускать второй акт, когда входящих нет

## 5. Инструменты MCP

- [ ] 5.1 `inbox_add` — `title`, `summary`, `source_kind`, `source_url`, `source_ref`; выжимка попадает в `agentNotes`, не в `notes`
- [ ] 5.2 Дедупликация в `inbox_add` по `source.ref`: совпал — обновляем существующее, не совпал или отсутствует — создаём новое
- [ ] 5.3 `inbox_list` — только записи с `kind: 'inbox'`
- [ ] 5.4 `task_comment` — дописывает в `agentNotes` существующей задачи, адресация та же, что у остальных инструментов
- [ ] 5.5 Убедиться, что `task_comment` не трогает поле `notes`

## 6. Проверка до включения агентского потока

- [ ] 6.1 Проверить на данных без входящих: приложение выглядит и работает как раньше
- [ ] 6.2 Обновить приложение на Mac и на телефоне, убедиться, что service worker подхватил новую оболочку на **обоих** устройствах
- [ ] 6.3 Создать входящее вручную, пройти разбор: в задачу, отложить, отбросить с отменой
- [ ] 6.4 Проверить, что входящее не появляется в разделах «Сегодня», «Все» и «Без даты»
- [ ] 6.5 Проверить синхронизацию входящего между устройствами

## 7. Включение агентского потока

- [ ] 7.1 Попросить агента положить первое входящее через `inbox_add`, пройти разбор целиком
- [ ] 7.2 Проверить дедупликацию: повторный `inbox_add` с тем же `source_ref` обновляет запись, второй не появляется
- [ ] 7.3 Проверить `task_comment` на существующей задаче
- [ ] 7.4 Описать раздел и три инструмента в `README.md` и `README.ru.md`
- [ ] 7.5 Записать в `README` правило: агент наполняет «Входящие» только по просьбе или по явно оговорённым правилам разбора
- [ ] 7.6 Поправить строку про горячие клавиши в `README.ru.md` — диапазон `1`…`7`
