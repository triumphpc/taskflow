## 1. Модель данных

- [x] 1.1 Добавить в `normalizeTask` (`js/store.js`) поле `kind` со значением `'task'` по умолчанию, если поле отсутствует
- [x] 1.2 Добавить нормализацию `source` вида `{ kind, url, title, ref, at }` с допустимыми `kind`: `gmail`, `telegram`, `web`, `calendar`, `agent`, `manual`
- [x] 1.3 Добавить нормализацию `agentNotes` как массива `{ at, text }`; пустой массив по умолчанию
- [x] 1.4 Не хранить `kind` вовсе, а выводить его из `done` и `due` (`deriveKind`): у входящего `due` пуст по
      определению, обнулять нечего, а `priority` сохраняется в записи — сброс стёр бы приоритет,
      проставленный человеком до этой работы. Скрывать приоритет у входящего — задача интерфейса (3.5)
- [x] 1.5 Ввести в `normalizeTask` инвариант: активная запись с пустым `due` получает `kind: 'inbox'`
- [x] 1.6 Исключить из инварианта выполненные записи — завершённая задача без даты остаётся в «Выполнено»
- [x] 1.7 Убедиться, что `SCHEMA` осталась `1` и `sync.mjs` не потребовал правок
- [x] 1.8 Проверить, что запись с датой и без `kind`, созданная до этой работы, читается как задача
- [x] 1.9 Проверить, что активные записи без даты из старых данных стали входящими и ни одна не потерялась

## 2. Выборки и раздел

- [x] 2.1 Добавить `VIEWS.inbox` в `js/model.js` с заголовком «Входящие»
- [x] 2.2 Исключить записи с `kind: 'inbox'` из всех остальных разделов — в том числе из «Без даты», куда они иначе провалятся по отсутствию `due`
- [x] 2.3 Добавить выборку неразобранных входящих (порядок — от свежего к старому по `source.at`) и счётчик для навигации
- [x] 2.4 Реализовать превращение входящего в задачу: `kind` → `'task'`, `source` и `agentNotes` сохраняются
- [x] 2.5 Поставить входящие в начало очереди `momentsCandidates()`, перед просроченными; порядок задач между собой не менять
- [x] 2.6 Убрать `VIEWS.someday`, ветку `groupsForView('someday')` и группу «Без даты» из раздела «Все задачи»
- [x] 2.7 Убрать ветку «задачи без даты» из `momentsCandidates()` — таких записей больше не существует
- [x] 2.8 Убрать счётчик `someday` из подсчёта разделов

## 3. Интерфейс

- [x] 3.1 Добавить `'inbox'` в `NAV_ITEMS` между `today` и `tomorrow`, и в `NAV_MOBILE` после `today`
- [x] 3.2 Показать счётчик неразобранных у пункта навигации; скрывать счётчик, когда разбирать нечего
- [x] 3.3 ~~Клавиша `7`~~ — отменено: спека закрепляет `5` за «Входящими», см. 3.11
- [x] 3.4 Сделать карточку входящего: источник с датой, заголовок, лента `agentNotes`, действия «В задачу», «Отбросить», переход к источнику `↗`
- [x] 3.5 Не показывать на карточке ни дату, ни приоритет и не давать их задать
- [x] 3.6 Показывать `agentNotes` и `notes` раздельно, различимо по происхождению
- [x] 3.7 Реализовать «В задачу» — открывает обычный редактор с датой и приоритетом
- [x] 3.8 Реализовать «Отбросить» — обычное удаление с надгробием и отменой через тост
- [x] 3.9 Добавить стили карточки в `styles.css`
- [x] 3.10 Убрать `'someday'` из `NAV_ITEMS`, подпись раздела и его пустое состояние; добавить пустое состояние «Входящих»
- [x] 3.11 Перевесить клавишу `5` с `someday` на `inbox` в `js/app.js`
- [x] 3.12 Убрать `defaultDateForView` для несуществующего раздела и проверить, что создание из «Все задачи» даёт входящее
- [x] 3.13 Поднять `CACHE` в `sw.js`

## 4. Moments

- [x] 4.1 Показывать в шапке карточки входящего источник и ленту `agentNotes`; вопросы «когда» и «приоритет» оставить теми же, что у задачи
- [x] 4.2 Сделать так, чтобы ответ на «когда» у входящего менял `kind` на `'task'` — отдельной кнопки «В задачу» в Moments не заводить
- [x] 4.3 Проверить, что «Пропустить» оставляет запись входящей, а «Отбросить» удаляет с надгробием
- [x] 4.4 Проверить, что при пустых входящих проход выглядит и работает ровно как раньше

## 5. Инструменты MCP

- [x] 5.1 `inbox_add` — `title`, `summary`, `source_kind`, `source_url`, `source_ref`; выжимка попадает в `agentNotes`, не в `notes`
- [x] 5.2 Убедиться, что `inbox_add` не принимает ни даты, ни приоритета — ни параметром, ни через общий путь обновления записи
- [x] 5.3 Дедупликация в `inbox_add` по `source.ref`: совпал — обновляем существующее, не совпал или отсутствует — создаём новое
- [x] 5.4 `inbox_list` — только записи с `kind: 'inbox'`
- [x] 5.5 Убрать `someday` из набора разделов `tasks_list`
- [x] 5.6 Проверить, что `task_add` без даты создаёт входящее, а не задачу
- [x] 5.7 `task_comment` — дописывает в `agentNotes` существующей задачи, адресация та же, что у остальных инструментов
- [x] 5.8 Убедиться, что `task_comment` не трогает поле `notes`

## 6. Проверка до включения агентского потока

- [x] 6.1 Проверить на данных без входящих: приложение выглядит и работает как раньше
- [ ] 6.2 Обновить приложение на Mac и на телефоне, убедиться, что service worker подхватил новую оболочку на **обоих** устройствах
- [x] 6.3 Создать входящее вручную, пройти разбор через Moments: дата и приоритет проставились, запись стала задачей
- [x] 6.4 Проверить разбор через карточку раздела: «В задачу» и «Отбросить» с отменой
- [x] 6.5 Проверить, что входящее не появляется в разделах «Сегодня» и «Все задачи»
- [x] 6.6 Создать задачу без даты и убедиться, что она попала во «Входящие»
- [x] 6.7 Снять дату у существующей задачи и убедиться, что она вернулась во «Входящие»
- [ ] 6.8 Проверить на реальных данных, что после обновления число активных записей не изменилось
- [x] 6.9 Проверить порядок очереди Moments: входящие идут перед просроченными
- [ ] 6.10 Проверить синхронизацию входящего между устройствами

## 7. Включение агентского потока

- [ ] 7.1 Попросить агента положить первое входящее через `inbox_add`, пройти разбор целиком
- [ ] 7.2 Проверить дедупликацию: повторный `inbox_add` с тем же `source_ref` обновляет запись, второй не появляется
- [ ] 7.3 Проверить `task_comment` на существующей задаче
- [x] 7.4 Описать раздел и три инструмента в `README.md` и `README.ru.md`
- [x] 7.5 Записать в `README` правило: агент наполняет «Входящие» только по просьбе или по явно оговорённым правилам разбора, и кладёт туда сырьё без даты и приоритета
- [x] 7.6 Поправить строку про горячие клавиши в `README.ru.md` — `5` ведёт во «Входящие»
- [x] 7.7 Убрать `someday` из таблиц `tasks_list` в `README.md` и `README.ru.md`
