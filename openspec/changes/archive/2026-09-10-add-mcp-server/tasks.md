## 1. Разрез `js/util.js` (самостоятельный шаг, ничего не ломает)

- [x] 1.1 Создать `js/core.js` — перенести `uid`, `WEEKDAYS_SHORT`, `WEEKDAYS_FULL`, `MONTHS_GEN`, `toDateStr`, `fromDateStr`, `todayStr`, `addDaysStr`, `nextMondayStr`, `isAllDay`, `datePart`, `timePart`, `combineDue`, `parseDue`, `isOverdue`, `daysBetween`, `fmtDue`, `fmtDayLabel`, `fmtDateShort`, `fmtTime`, `plural`, `oneLine`
- [x] 1.2 Создать `js/dom.js` — перенести `$`, `$$`, `h`, `append`, `clear`, `debounce`, `findLinks`, `linkify`, `caretIndexAt`
- [x] 1.3 Убедиться, что в `js/core.js` не встречается ни `document`, ни `window` (`grep -n "document\|window" js/core.js` возвращает пусто)
- [x] 1.4 Удалить `js/util.js` полностью, не оставляя ре-экспорта — забытый импорт должен падать сразу
- [x] 1.5 Поправить импорты в `js/ui.js`, `js/model.js`, `js/store.js`, `js/moments.js`, `js/app.js`, `js/sync.js`
- [x] 1.6 Обновить `SHELL` в `sw.js` (`./js/util.js` → `./js/core.js` и `./js/dom.js`), поднять `CACHE`
- [x] 1.7 Проверить приложение в браузере: разделы, создание задачи, повторы, Moments, синхронизация — всё работает как до разреза

## 2. Каркас зависимостей

- [x] 2.1 Создать `package.json` с единственной зависимостью `@modelcontextprotocol/sdk`, тип `module`
- [x] 2.2 Добавить `node_modules` в `.gitignore`
- [x] 2.3 Проверить, что на чистой копии без `npm install` команда `node serve.mjs` поднимается и приложение работает полностью

## 3. `mcp.mjs`: транспорт и доступ

- [x] 3.1 Поднять сервер Streamable HTTP на маршруте `POST /mcp`, порт из `PORT` (по умолчанию 8788), слушать только `127.0.0.1`
- [x] 3.2 Отвечать `405` на `GET /mcp`
- [x] 3.3 Читать токен: `TASKFLOW_MCP_TOKEN` → `data/mcp-token.txt` → сгенерировать при первом запуске и записать в файл
- [x] 3.4 Проверять `Authorization: Bearer`, сравнение в постоянное время — перенести приём из `sync.mjs`
- [x] 3.5 Проверять заголовок `Origin`, отклонять чужой
- [x] 3.6 Клиент к основному API: `GET /api/state` и `POST /api/sync` с sync-токеном, адрес из `TASKFLOW_API`
- [x] 3.7 Оборачивать недоступность `serve.mjs` в понятный текст ошибки вместо стектрейса

## 4. `mcp.mjs`: инструменты

- [x] 4.1 Импортировать даты и `uid` из `js/core.js`; не писать собственной реализации разбора срока, «ближайшего понедельника» и просроченности
- [x] 4.2 Общая адресация задачи: по началу id или фрагменту названия; одно совпадение — действуем, несколько — возвращаем кандидатов и ничего не меняем, ноль — внятная ошибка
- [x] 4.3 `tasks_list` — разделы `today`, `tomorrow`, `upcoming`, `someday`, `all`, `done`, `overdue`; флаг `verbose`
- [x] 4.4 `task_get` — одна задача целиком, с заметками и подзадачами
- [x] 4.5 `task_add` — `title`, `notes`, `due`, `priority`, `subtasks[]`, `repeat`, `every`
- [x] 4.6 `task_edit` — изменяемые поля плюс `clear_due`
- [x] 4.7 `task_snooze` — перенос даты с сохранением времени суток; задача на весь день остаётся на весь день
- [x] 4.8 `task_done` — выполнено и обратно в работу через `undo`
- [x] 4.9 `subtask_add` и `subtask_toggle`
- [x] 4.10 `task_delete` — без `confirm: true` возвращает ошибку с названием задачи и ничего не трогает; с подтверждением удаляет с надгробием

## 5. Локальная проверка

- [x] 5.1 Поднять `serve.mjs` и `mcp.mjs` локально, подключить MCP-клиента к `http://127.0.0.1:8788/mcp`
- [x] 5.2 Прогнать все девять инструментов; убедиться, что созданное агентом видно в браузере
- [x] 5.3 Проверить неоднозначную адресацию: фрагмент, под который подходят несколько задач, возвращает кандидатов и не меняет данные
- [x] 5.4 Проверить `task_delete` без `confirm` — задача на месте, в ошибке её название
- [x] 5.5 Проверить состав `overdue` и разбор срока против того, что показывает приложение в тот же момент
- [x] 5.6 Погасить `serve.mjs`, вызвать инструмент — получить понятную ошибку, не стектрейс
- [x] 5.7 Проверить `401` на запрос без токена и на запрос со старым токеном после ротации

## 6. Выкатка на VPS

- [x] 6.1 Завести `/etc/systemd/system/taskflow-mcp.service` (`PORT=8788`, `TASKFLOW_API=http://127.0.0.1:8787`, `TASKFLOW_DATA=/var/lib/taskflow`, `After=taskflow.service`, `Restart=always`)
- [x] 6.2 Добавить в Caddy `handle /mcp* { reverse_proxy 127.0.0.1:8788 }` перед общим `handle`
- [x] 6.3 Выполнить `npm install` на сервере, запустить юнит, проверить логи
- [x] 6.4 Подключить агента к `https://v864391.hosted-by-vdsina.com/mcp`, прогнать сценарии из группы 5
- [x] 6.5 Убедиться, что ротация MCP-токена отзывает доступ агенту и не задевает браузер на телефоне и Mac

## 7. Документация и переезд скилла

- [x] 7.1 Описать в `README.md` и `README.ru.md` MCP-сервер, второй токен, команду подключения `claude mcp add --transport http`
- [x] 7.2 Записать в `README` отклонение от OAuth 2.1 — что не реализован и почему
- [x] 7.3 **Проверить работу под корпоративным VPN.** Если домен не резолвится — прописать строку в `/etc/hosts` и задокументировать это как обязательный шаг настройки под VPN
- [x] 7.4 Переписать `SKILL.md` в `~/Devel/agents-ensemble/skills/taskflow/` с CLI на MCP-инструменты (правка в другом репозитории)
- [x] 7.5 Удалить `tf.py` и `__pycache__` там же — только после того, как MCP проверен, в том числе под VPN
